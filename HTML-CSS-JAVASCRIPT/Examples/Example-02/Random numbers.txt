Generating random numbers

var random_number = Math.random(); (range is b/w 0-1)

through multiplication, the range can be modified
e.g. var random = Math.random() * 3 (range is from 0 - 2.999999..)

Math.floor() can be used to return an integer
e.g. var random = Math.floor(Math.random() * 5);